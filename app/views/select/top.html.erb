<div class="container-mt-5">
  <div class="row">
    <div class="col-6 mx-auto">
      <h1 class="top-title"><span>要素を3つ選択してください</span></span></h1>

      <% @elements.each do |element| %>
        <%# todoのcompleteカラムをis_finishedパスで呼び出し、表示を変更する %>
        <% if element.check == false %>
        <%= link_to element.check, select_element_path(element), method: :put, class: "btn btn-outline-info" %>
        <% else %>
        <%= link_to element.check, release_element_path(element), method: :put, class: "btn btn-info" %>
        <% end %>
      <% end %>
      
      <%= link_to  "#", class: "btn btn-outline-dark btn-item" do %>
      診断結果を見る
      <% end %>

    </div>
  </div>
</div>